# Placeholder values. DO NOT commit real credentials.
# Replace via: oc create secret generic elastic-credentials \
#   --from-literal=ES_ENDPOINT=https://your-cluster.es.io \
#   --from-literal=ES_API_KEY=your-api-key \
#   --dry-run=client -o yaml | oc apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: elastic-credentials
type: Opaque
stringData:
  ES_ENDPOINT: "https://YOUR_ES_ENDPOINT"
  ES_API_KEY: "YOUR_API_KEY"
